schema: http://localhost:4000/graphql

documents:
  - client/src/**/*.graphql

generates:
  client/src/shared/graphql/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-rtk-query
    config:
      importBaseApiFrom: "../../features/api/api"
      exportHooks: true
      addTagTypes: true